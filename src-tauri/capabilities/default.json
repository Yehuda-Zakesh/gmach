{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline permissions for the main catalog window, the admin window, and the preview window",
  "windows": ["main", "admin", "preview"],
  "permissions": ["core:default", "dialog:default"]
}
